import {sqliteTable,text,integer,index} from 'drizzle-orm/sqlite-core';
export const profiles=sqliteTable('profiles',{id:text('id').primaryKey(),name:text('name').notNull(),coins:integer('coins').notNull().default(450),owned:text('owned').notNull(),equipped:text('equipped').notNull(),adminUntil:integer('admin_until').notNull().default(0)});
export const runs=sqliteTable('runs',{id:text('id').primaryKey(),player:text('player').notNull(),name:text('name').notNull(),level:integer('level').notNull(),started:integer('started').notNull(),ended:integer('ended'),duration:integer('duration'),status:text('status').notNull().default('playing'),correct:integer('correct').notNull().default(0),loadout:text('loadout').notNull(),admin:integer('admin').notNull().default(0)},t=>[index('idx_runs_level_ended').on(t.level,t.ended)]);
